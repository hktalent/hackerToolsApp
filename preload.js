window.addEventListener('DOMContentLoaded', () => {
  var o = document.getElementById('sOther')
  for(var k in process.versions)
  {
    // if(process.versions[k])o.innerText += "\n" + k + " = " + process.versions[k]
  }
})
